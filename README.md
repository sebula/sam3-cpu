# sam3-cpu

Stripped **CPU inference** fork of [facebookresearch/sam3](https://github.com/facebookresearch/sam3). Install: `pip install -e .` (needs `torch` / `torchvision`; checkpoints load from Hugging Face). License: [LICENSE](LICENSE).
